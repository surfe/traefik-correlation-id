displayName: Traefik Correlation ID
type: middleware

import: github.com/surfe/traefik-correlation-id

summary: Injects an correlation-id header into every request, generating a UUID v4 if one is not already present.

testData:
  headerName: correlation-id
